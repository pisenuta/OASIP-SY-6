version: '3.7'
services:
  oasip:
    build: ./
    image: frontendimg
    container_name: frontend
    ports:
      - 3000:3000
    networks: 
      - oasip-network
    restart: always
    environment:
      TZ: "Asia/Bangkok"
    
networks:
  oasip-network:
    external:
     name: oasip-network